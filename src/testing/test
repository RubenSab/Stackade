"corelibs/strings" run
"corelibs/printing" run
"corelibs/higherOrder" run

# a: cy
# c: a
# y: aaa

"computeNext" (
    dup 0 strAt
    {
        (dup "a" ==)
        ("cy" swap pop)
        ({
            ("c" ==)
            ("a")
            ("aaa")
        })
    }
    strCat 2 sliceRight
) :seq


"computeUntilEnd" (
    dup println
    {
        (dup strLen 1 >)
        (computeNext dup println self)
    }
) :seq

"yyyy" computeUntilEnd debug